<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>View Merchant</title>
</head>
<body>
  <h1>Merchant Details</h1>
  <p><strong>Name:</strong> <%= merchant.user_id.name %></p>
  <p><strong>Email:</strong> <%= merchant.user_id.email %></p>
  <p><strong>Business Name:</strong> <%= merchant.business_name %></p>
  <p><strong>MSME ID:</strong> <%= merchant.msme_id %></p>
  <p><strong>GSTIN:</strong> <%= merchant.gstin %></p>
  <p><strong>Address:</strong> <%= merchant.business_address.street %>, <%= merchant.business_address.city %>, <%= merchant.business_address.state %>, <%= merchant.business_address.postal_code %>, <%= merchant.business_address.country %></p>
  <p><strong>Verification Status:</strong> <%= merchant.verification_status %></p>
  <h2>Verification Documents</h2>
  <ul>
    <% merchant.verification_documents.forEach(doc => { %>
      <li><a href="<%= doc %>" target="_blank">View Document</a></li>
    <% }) %>
  </ul>
  <form action="/api/admin/verify-merchant/<%= merchant._id %>" method="POST">
    <button type="submit">Verify Merchant</button>
  </form>
</body>
</html>
